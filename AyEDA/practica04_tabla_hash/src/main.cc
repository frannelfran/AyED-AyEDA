#include "options/options.h"
#include "nif/nif.h"

using namespace std;

int main(int argc, char* argv[]) {
  auto options = parse_args(argc, argv);
  srand(time(nullptr));

  
  
  
  










  return 0;
}